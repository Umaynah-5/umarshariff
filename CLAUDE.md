# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Repository Status

This repository is currently empty. No source files, build configuration, tests, or tooling have been committed yet.

When the project is initialized, update this file with:

- **Build & run commands** — how to install dependencies, start a dev server, build for production
- **Test commands** — how to run the full suite and a single test file
- **Lint/format commands** — the linter(s) in use and how to run them
- **Architecture overview** — the high-level structure (e.g. monorepo layout, key packages, data flow between layers)
- **Key conventions** — naming patterns, file organization rules, or anything non-obvious that isn't enforced by a linter
